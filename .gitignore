# Dependencies
lib/
node_modules/
/package-lock.json

# Build & artifacts
out/
dist/
cache/
broadcast/

# Env and secrets (never commit private keys)
.env
.env.*
!.env.example

# IDE / OS
.idea/
.vscode/
.DS_Store

# Test / temp
test-agent-out.txt
backend/test-agent-out.txt

# SMS registry (phone numbers — do not commit)
backend/data/
